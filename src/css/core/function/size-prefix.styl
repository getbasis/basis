//
// Return prefix for the device size
//
// @param   string  size  sm|md|lg
// @return  string
//
_size-prefix(size) {
  prefix = '-';

  if (size != 'sm') {
    prefix = '--' + size;
  }

  return prefix;
}
