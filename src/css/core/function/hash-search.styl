//
// Return matched key in the hash
//
// @param   string      key
// @param   hash        hash
// @return  mixed|null
//
_hash-search(key, hash) {
  for _key, _value in hash {
    if (_key == key) {
      return _key;
    }
  }
  return null;
}
