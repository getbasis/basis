* {
  &, &:before, &:after {
    box-sizing: border-box;
  }
}

html, body {
  margin: 0;
  padding: 0;
}

html {
  font-size: (_strip-unit(bs-base-font-size-px) / 16 * 100%);
}

body {
  color: _color-text;
  font-family: bs-font-family;
  _font-size-line-height(bs-base-font-size);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

code, kbd, pre, samp {
  _font-size-line-height(bs-base-font-size-px - 2);
  font-family: '_', monospace;
}

input, textarea, keygen, select, button {
  font-family: bs-font-family;
}

img {
  height: auto;
  max-width: 100%;
  vertical-align: middle;
}

p, ul, ol, dl, table, pre, blockquote {
  margin-top: 0;
  _margin-bottom(1);
}

h1, h2, h3, h4, h5, h6 {
  font-weight: bold;
}

h1 {
  _font-size-line-height(bs-base-font-size * bs-h1-font-size-scale);
  _margin-top(bs-h1-margin-top);
  _margin-bottom(bs-h1-margin-bottom);
}

h2 {
  _font-size-line-height(bs-base-font-size * bs-h2-font-size-scale);
  _margin-top(bs-h2-margin-top);
  _margin-bottom(bs-h2-margin-bottom);
}

h3 {
  _font-size-line-height(bs-base-font-size * bs-h3-font-size-scale);
  _margin-top(bs-h3-margin-top);
  _margin-bottom(bs-h3-margin-bottom);
}

h4 {
  _font-size-line-height(bs-base-font-size * bs-h4-font-size-scale);
  _margin-top(bs-h4-margin-top);
  _margin-bottom(bs-h4-margin-bottom);
}

h5 {
  _font-size-line-height(bs-base-font-size * bs-h5-font-size-scale);
  _margin-top(bs-h5-margin-top);
  _margin-bottom(bs-h5-margin-bottom);
}

h6 {
  _font-size-line-height(bs-base-font-size * bs-h6-font-size-scale);
  _margin-top(bs-h6-margin-top);
  _margin-bottom(bs-h6-margin-bottom);
}

ol ol, ol ul, ul ol, ul ul {
  margin-bottom: 0;
}

dt {
  font-weight: bold;
}
dd {
  margin-left: 0;
}

blockquote {
  border-left: (bs-base-font-size / 2) solid _lighter(_color-gray);
  margin-left: 0;
  padding: bs-base-font-size 0 bs-base-font-size bs-base-font-size;

  :last-child {
    margin-bottom: 0;
  }

  footer {
    color: _darker(_color-gray);
    font-size: 90%;
  }
}

code {
  background-color: _lighter(_color-red);
  border-radius: _border-radius;
  color: _color-red;
  padding: 2px 4px;
}

pre {
  background-color: _lightest(_color-gray);
  border: 1px solid _lighter(_color-gray);
  border-radius: _border-radius;
  padding: bs-base-font-size;
  overflow: auto;

  code {
    background-color: transparent;
    border: none;
    color: _color-text;
    font-size: 100%;
    padding: 0;
  }
}

label {
  cursor: pointer;
}
