.{bs-prefix}c-media {
  @extend $_media;

  &__body {
    > .{bs-prefix}c-media {
      bs-margin-top(1);
    }
  }

  &--middle {
    align-items: center;
  }

  &--reverse {
    .{bs-prefix}c-media__figure {
      bs-margin(0, 0, 0, 1);
      order: 1;
    }
  }
}
