//
// Return the key of hash
//
// @param   string  key
// @param   hash    hash
// @return  mixed or null
//
_hash-search(key, hash) {
  for _key, _value in hash {
    if (_key == key) {
      return _key;
    }
  }
  return null;
}
