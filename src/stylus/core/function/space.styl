//
// Return the margin conforming to the rhythm
//
// @param   int     coefficient
// @return  length
//
_space(coefficient) {
  margin     = (_base-line-height * coefficient);
  add-margin = (_base-line-height * (_margin-coefficient - 1));
  return (margin + _margin-coefficient);
}
