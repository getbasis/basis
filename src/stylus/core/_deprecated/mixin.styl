//
// Adding margin for the row
//
// @param  int  coefficient
//
bs-row-margin(coefficient) {
  _row--margin(coefficient);
}

//
// Adding margin for the column
//
// @param  int  coefficient
//
bs-col-margin(coefficient) {
  _row__col--margin(coefficient);
}

//
// Setting column width
//
// @param  length  width
//
bs-col-width(width) {
  _row__col--width(width);
}

//
// Setting column width
//
// @param  int  columns  molecule column size
// @param  int  max-columns  denominator column size
//
bs-col(columns = null, max-columns = null) {
  _row__col(columns, max-columns);
}

//
// Setting column offset
//
// @param  int  columns  molecule offset size
// @param  int  max-columns  denominator offset size
//
bs-col-offset(columns = null, max-columns = null) {
  _row__col--offset(columns, max-columns);
}

//
// Bounce vertical
//
// @param   length             px        bounce size
// @param   second|milisecond  delay: 0  animate time
//
bs-bounce-vertical(px, delay = 0) {
  _bounce-vertical(px, delay);
}

//
// Shake vertical
//
bs-shake-vertical() {
  _shake-vertical();
}

//
// Vibrate vertical
//
bs-vibrate-vertical() {
  _vibrate-vertical();
}

//
// Bounce horizontal
//
// @param   length             px        bounce size
// @param   second|milisecond  delay: 0  animate time
//
bs-bounce-horizontal(px, delay = 0) {
  _bounce-horizontal(px, delay);
}

//
// Shake horizontal
//
bs-shake-horizontal() {
  _shake-horizontal();
}

//
// Vibrate horizontal
//
bs-vibrate-horizontal() {
  _vibrate-horizontal();
}

//
// Bounce scale
//
// @param   float              px        bounce size
// @param   second|milisecond  delay: 0  animate time
//
bs-bounce-scale(float, delay = 0) {
  _bounce-scale(float, delay);
}

//
// Shake scale
//
bs-shake-scale() {
  _shake-scale();
}

//
// Vibrate scale
//
bs-vibrate-scale() {
  _vibrate-scale();
}

//
// Extend the underline from the center.
// This mixin use the pseudo-elements.
//
bs-extend-underline() {
  _extend-underline();
}

//
// Background image cover + centering
//
bs-background-image-cover() {
  _background-image-cover();
}

//
// Background image fixed
//
bs-background-image-fixed() {
  _background-image-fixed();
}

//
// Clearfix helper
//
// .clerfix {
//   bs-clearfix();
// }
//
// <div class="clearfix">
//   <img style="float: right">
//   ....
// </div>
//
bs-clearfix() {
  _clearfix();
}

//
// Event helper
//
// img {
//   +bs-hover() {
//     opacity: .8;
//   }
// }
//
bs-hover() {
  _hover();
}

//
// Ghost style
//
// .c-btn-ghost {
//   @extend $_btn;
//   bs-ghost();
// }
//
// <a class="c-btn-ghost" role="button">btn</a>
//

bs-ghost() {
  _ghost();
}
